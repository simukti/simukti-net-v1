<?php if($this->result) : ?>

<?php 
$title = sprintf("Article / Tag: %s", $this->tag);
$this->headTitle($title); 
?>

<div class="content single-view">
    <?php echo $this->render('simukti/article/_list', array(
        'title'     => $this->tag,
        'articles'  => $this->result['data'],
        'icon'      => 'icon-tags'
    ), array('force_canonical' => true)); ?>
</div>

<?php endif; ?>